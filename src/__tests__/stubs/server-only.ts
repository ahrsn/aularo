// Test-only stub for `server-only`. In production this module throws when
// imported outside server code; under Vitest we treat it as a no-op so pure
// units of server code can still be exercised.
export {};
